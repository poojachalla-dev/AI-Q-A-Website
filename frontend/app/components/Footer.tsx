export default function Footer() {
  return (
    <footer
      className="mono"
      style={{
        borderTop: "1px solid var(--line)",
        padding: "32px 24px",
        textAlign: "center",
        fontSize: 11,
        letterSpacing: "0.04em",
        color: "var(--mist-dim)",
      }}
    >
      AI INTERVIEW PREP · A KNOWLEDGE GRAPH, NOT A SPREADSHEET
    </footer>
  );
}
